# Compose to Quadlet

This guide will cover variable-free conversion, successful explicit interpolation, unresolved
source-variable diagnostics, target-version boundaries, and applicable loss policies.

The final guide will use repository-owned fixtures and black-box CLI tests for every displayed
command.

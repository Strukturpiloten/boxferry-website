# Compose to Compose

This guide will show canonical Compose output both with preserved interpolation expressions and
with explicit `--interpolate` processing through `--env-file` and `--env`.

The final guide will use repository-owned fixtures and black-box CLI tests for every displayed
command.

# CLI reference

The CLI reference will be generated from or checked against BoxFerry's executable Clap contract so
commands, options, defaults, value sets, and route-specific help cannot silently drift.
